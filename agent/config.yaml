# node identity
node:
  id: node002
  hostname: dev-machine
  display_name: cxan

# backend service
server:
  url: http://192.168.21.12:50001
  register_endpoint: /api/agent/register
  metrics_endpoint: /api/agent/metrics
  agent_token: CHANGE_ME_AGENT_TOKEN

# collection config
collection:
  interval: 1
  retry: 3
  timeout: 5

# logging
logging:
  level: INFO
  file: agent.log
