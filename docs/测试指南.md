# 测试指南 - 分布式感知运维系统

**版本**: 1.0  
**更新时间**: 2026-02-07

---

## 📋 测试概览

本文档提供完整的系统测试指南，包括单元测试、集成测试、端到端测试和性能测试。

---

## 🚀 快速测试（推荐）

### 自动化测试脚本

项目提供了自动化测试脚本，可以快速检查系统状态：

**Windows (推荐使用 PowerShell 中文版)**:
```powershell
# PowerShell 中文版（推荐，支持中文显示）
.\test-all-cn.ps1

# CMD 英文版（避免乱码）
.\test-all.bat
```

**Linux/Mac**:
```bash
chmod +x test-all.sh
./test-all.sh
```

**注意事项**:
- Windows CMD 可能显示中文乱码，建议使用 PowerShell 运行 `test-all-cn.ps1`
- 如果 PowerShell 提示执行策略错误，运行：`Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned`
- `test-all.bat` 使用英文输出，避免乱码问题

**测试内容**:
- ✅ 后端数据库连接
- ✅ 后端依赖完整性
- ✅ 前端依赖完整性
- ✅ Agent Python 语法
- ✅ Python 依赖安装

**预期输出**:
```
========================================
EOMS 系统测试脚本
========================================

[1/5] 测试后端数据库连接...
✅ 后端连接测试通过

[2/5] 检查后端依赖...
✅ 后端依赖完整

[3/5] 检查前端依赖...
✅ 前端依赖完整

[4/5] 检查 Agent Python 语法...
✅ Agent 语法检查通过

[5/5] 检查 Python 依赖...
✅ psutil 已安装
✅ requests 已安装
✅ pyyaml 已安装

========================================
测试完成！
========================================
```

**测试结果**:
- 如果所有测试通过，可以继续启动系统
- 如果测试失败，请查看 [测试报告.md](./测试报告.md) 和 [问题修复清单.md](./问题修复清单.md)

---

## 🧪 测试环境准备

### 1. 安装依赖

```bash
# 后端依赖
cd backend
npm install

# 前端依赖
cd frontend
npm install

# Agent 依赖
cd agent
pip install -r requirements.txt
```

### 2. 启动基础服务

```bash
# 启动 Redis (可选)
redis-server

# 启动 InfluxDB (可选)
influxd
```

### 3. 配置环境变量

确保 `backend/.env` 配置正确：
```env
PORT=3000
JWT_SECRET=your-secret-key
REDIS_HOST=localhost
REDIS_PORT=6379
INFLUX_URL=http://localhost:8086
INFLUX_TOKEN=your-token
INFLUX_ORG=eoms
INFLUX_BUCKET=metrics
```

---

## 🔧 后端测试

### 测试 1: 数据库连接测试

**目的**: 验证 Redis 和 InfluxDB 连接

```bash
cd backend
node test-connection.js
```

**预期结果**:
```
✓ Redis 连接成功
✓ InfluxDB 连接成功
```

**故障排查**:
- Redis 连接失败: 检查 Redis 服务是否启动
- InfluxDB 连接失败: 检查 Token 和 URL 配置

---

### 测试 2: 后端服务启动测试

**目的**: 验证后端服务正常启动

```bash
cd backend
npm run dev
```

**预期结果**:
```
Server running on port 3000
✓ Redis connected
✓ InfluxDB connected
WebSocket server ready
```

**验证检查**:
- [ ] 服务在 3000 端口启动
- [ ] Redis 连接成功
- [ ] InfluxDB 连接成功
- [ ] WebSocket 服务就绪

---

### 测试 3: API 接口测试

#### 3.1 健康检查

```bash
curl http://localhost:3000/health
```

**预期响应**:
```json
{
  "status": "ok",
  "timestamp": "2026-02-07T10:00:00.000Z"
}
```

#### 3.2 用户登录

```bash
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "password": "admin"
  }'
```

**预期响应**:
```json
{
  "success": true,
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "id": "1",
    "username": "admin"
  }
}
```

**保存 Token**: 将返回的 token 用于后续请求

#### 3.3 Agent 注册

```bash
curl -X POST http://localhost:3000/api/agent/register \
  -H "Content-Type: application/json" \
  -d '{
    "nodeId": "test-node-001",
    "hostname": "test-machine",
    "ip": "192.168.1.100",
    "os": "Linux",
    "arch": "x64"
  }'
```

**预期响应**:
```json
{
  "success": true,
  "message": "Agent registered successfully",
  "nodeId": "test-node-001"
}
```

#### 3.4 上报监控数据

```bash
curl -X POST http://localhost:3000/api/agent/metrics \
  -H "Content-Type: application/json" \
  -d '{
    "nodeId": "test-node-001",
    "timestamp": "2026-02-07T10:00:00.000Z",
    "cpu": {
      "usage": 45.2,
      "cores": 8
    },
    "memory": {
      "total": 16384,
      "used": 8192,
      "usage": 50.0
    },
    "disk": {
      "total": 512000,
      "used": 256000,
      "usage": 50.0
    },
    "network": {
      "bytesIn": 1024000,
      "bytesOut": 512000
    }
  }'
```

**预期响应**:
```json
{
  "success": true,
  "message": "Metrics received"
}
```

#### 3.5 获取节点列表

```bash
curl http://localhost:3000/api/agent/list
```

**预期响应**:
```json
{
  "success": true,
  "nodes": [
    {
      "nodeId": "test-node-001",
      "hostname": "test-machine",
      "ip": "192.168.1.100",
      "status": "online",
      "lastSeen": "2026-02-07T10:00:00.000Z"
    }
  ]
}
```

#### 3.6 获取最新数据

```bash
curl http://localhost:3000/api/metrics/latest/test-node-001
```

**预期响应**:
```json
{
  "success": true,
  "data": {
    "nodeId": "test-node-001",
    "timestamp": "2026-02-07T10:00:00.000Z",
    "cpu": { "usage": 45.2 },
    "memory": { "usage": 50.0 },
    "disk": { "usage": 50.0 }
  }
}
```

#### 3.7 查询历史数据

```bash
curl "http://localhost:3000/api/metrics/history?nodeId=test-node-001&startTime=2026-02-07T09:00:00Z&endTime=2026-02-07T10:00:00Z"
```

**预期响应**:
```json
{
  "success": true,
  "data": [
    {
      "timestamp": "2026-02-07T09:00:00.000Z",
      "cpu_usage": 45.2,
      "memory_usage": 50.0,
      "disk_usage": 50.0
    }
  ]
}
```

---

### 测试 4: WebSocket 连接测试

使用浏览器控制台或 WebSocket 客户端工具：

```javascript
// 在浏览器控制台执行
const ws = new WebSocket('ws://localhost:3000/ws/metrics');

ws.onopen = () => {
  console.log('✓ WebSocket 连接成功');
  
  // 订阅节点数据
  ws.send(JSON.stringify({
    type: 'subscribe',
    nodeId: 'test-node-001'
  }));
};

ws.onmessage = (event) => {
  const data = JSON.parse(event.data);
  console.log('收到数据:', data);
};

ws.onerror = (error) => {
  console.error('✗ WebSocket 错误:', error);
};

ws.onclose = () => {
  console.log('WebSocket 连接关闭');
};
```

**预期结果**:
- 连接成功
- 订阅成功
- 收到实时数据推送

---

## 🤖 Agent 测试

### 测试 5: Agent 启动测试

**目的**: 验证 Agent 正常启动和数据采集

```bash
cd agent
python agent.py
```

**预期输出**:
```
EOMS Agent starting...
Node ID: node001
Hostname: your-machine
Agent registered successfully
✓ CPU: 45.2% | MEM: 68.5% | DISK: 72.3%
✓ CPU: 46.1% | MEM: 68.7% | DISK: 72.3%
...
```

**验证检查**:
- [ ] Agent 成功启动
- [ ] 自动注册成功
- [ ] 每秒采集一次数据
- [ ] 数据成功上报
- [ ] 无错误日志

---

### 测试 6: Agent 配置测试

**目的**: 验证配置文件正确加载

编辑 `agent/config.yaml`:
```yaml
server:
  url: http://localhost:3000
  
agent:
  node_id: custom-node-001
  interval: 2
```

重启 Agent，验证：
- [ ] 使用自定义 node_id
- [ ] 采集间隔为 2 秒

---

### 测试 7: Agent 重试机制测试

**目的**: 验证网络异常时的重试机制

1. 启动 Agent
2. 停止后端服务
3. 观察 Agent 日志

**预期行为**:
```
✗ Failed to upload metrics: Connection refused
Retrying... (1/3)
✗ Failed to upload metrics: Connection refused
Retrying... (2/3)
```

4. 重启后端服务
5. Agent 应自动恢复上报

---

## 🎨 前端测试

### 测试 8: 前端启动测试

**目的**: 验证前端应用正常启动

```bash
cd frontend
npm run dev
```

**预期结果**:
```
VITE v5.x.x ready in xxx ms

➜  Local:   http://localhost:5174/
➜  Network: use --host to expose
```

**验证检查**:
- [ ] 应用在 5174 端口启动
- [ ] 无编译错误
- [ ] 无 TypeScript 错误

---

### 测试 9: 登录功能测试

**步骤**:
1. 访问 http://localhost:5174
2. 输入用户名: `admin`
3. 输入密码: `admin`
4. 点击登录

**预期结果**:
- [ ] 登录成功
- [ ] 跳转到首页
- [ ] Token 保存到 localStorage
- [ ] 侧边栏显示用户名

**故障排查**:
- 登录失败: 检查后端服务是否启动
- 网络错误: 检查 API 地址配置

---

### 测试 10: 实时监控测试

**前置条件**: 至少一个 Agent 在线

**步骤**:
1. 登录系统
2. 点击侧边栏 "监控面板"
3. 选择一个节点

**预期结果**:
- [ ] 节点列表正常显示
- [ ] 节点状态正确 (在线/离线)
- [ ] 实时数据每秒更新
- [ ] CPU/内存/磁盘进度条正常
- [ ] 趋势图表实时更新
- [ ] WebSocket 状态显示 "已连接"

**验证数据**:
- 打开浏览器开发者工具
- 查看 Network → WS 标签
- 应看到持续的数据推送

---

### 测试 11: 历史数据查询测试

**步骤**:
1. 点击侧边栏 "历史查询"
2. 选择节点
3. 选择时间范围 (例如: 最近 1 小时)
4. 点击 "查询"

**预期结果**:
- [ ] 查询按钮显示加载状态
- [ ] 数据成功加载
- [ ] 统计卡片显示正确
- [ ] 四个图表正常显示
- [ ] 数据表格显示完整数据

**测试导出功能**:
1. 点击 "导出 CSV"
2. 验证下载的 CSV 文件
3. 检查数据完整性

---

### 测试 12: 节点管理测试

**步骤**:
1. 在监控面板查看节点列表
2. 停止一个 Agent
3. 等待 30 秒

**预期结果**:
- [ ] 节点状态变为 "离线"
- [ ] 状态指示器变为灰色
- [ ] 最后更新时间停止更新

**恢复测试**:
1. 重启 Agent
2. 观察节点状态

**预期结果**:
- [ ] 节点状态变为 "在线"
- [ ] 数据恢复更新

---

## 🔗 集成测试

### 测试 13: 端到端数据流测试

**目的**: 验证完整的数据流转

**步骤**:
1. 启动 Redis
2. 启动后端服务
3. 启动 Agent
4. 启动前端应用
5. 登录系统
6. 打开监控面板

**验证数据流**:
```
Agent 采集 → HTTP POST → 后端接收 → Redis 缓存 → WebSocket 推送 → 前端显示
                                    ↓
                              InfluxDB 存储
```

**检查点**:
- [ ] Agent 日志显示上报成功
- [ ] 后端日志显示接收数据
- [ ] Redis 中有最新数据 (`redis-cli get node:test-node-001:latest`)
- [ ] 前端实时显示数据
- [ ] InfluxDB 中有历史数据

---

### 测试 14: 多节点并发测试

**目的**: 验证系统支持多节点

**步骤**:
1. 启动 3 个 Agent (不同 node_id)
2. 在前端查看节点列表

**预期结果**:
- [ ] 所有节点正常显示
- [ ] 每个节点数据独立更新
- [ ] 切换节点时数据正确显示
- [ ] 无数据混淆

---

### 测试 15: 长时间运行测试

**目的**: 验证系统稳定性

**步骤**:
1. 启动所有服务
2. 运行 1 小时
3. 观察系统状态

**监控指标**:
- [ ] 内存使用稳定 (无内存泄漏)
- [ ] CPU 使用正常
- [ ] WebSocket 连接稳定
- [ ] 无异常错误日志
- [ ] 数据持续更新

---

## ⚡ 性能测试

### 测试 16: API 响应时间测试

使用 Apache Bench 或类似工具：

```bash
# 测试登录接口
ab -n 1000 -c 10 -p login.json -T application/json \
  http://localhost:3000/api/auth/login

# 测试获取最新数据接口
ab -n 1000 -c 10 \
  http://localhost:3000/api/metrics/latest/test-node-001
```

**性能目标**:
- 登录接口: < 100ms (平均)
- 获取最新数据: < 50ms (平均)
- 历史查询: < 500ms (平均)

---

### 测试 17: WebSocket 并发连接测试

**目的**: 验证 WebSocket 支持多客户端

**步骤**:
1. 打开 10 个浏览器标签页
2. 每个标签页登录并打开监控面板
3. 观察系统表现

**预期结果**:
- [ ] 所有连接正常建立
- [ ] 数据推送正常
- [ ] 服务器资源使用合理

---

### 测试 18: 大数据量查询测试

**前置条件**: 系统运行 24 小时以上

**步骤**:
1. 查询 24 小时历史数据
2. 测量响应时间
3. 检查数据完整性

**性能目标**:
- 查询时间: < 2s
- 数据点数: > 86400 (24h * 3600s)
- 图表渲染: < 1s

---

## 🐛 故障测试

### 测试 19: 网络中断恢复测试

**场景 1: Agent 网络中断**
1. 启动 Agent
2. 断开网络
3. 等待 30 秒
4. 恢复网络

**预期行为**:
- Agent 自动重试
- 网络恢复后自动上报
- 无数据丢失

**场景 2: 前端网络中断**
1. 打开监控面板
2. 断开网络
3. 恢复网络

**预期行为**:
- WebSocket 自动重连
- 数据恢复更新
- 显示连接状态变化

---

### 测试 20: 服务重启测试

**步骤**:
1. 系统正常运行
2. 重启后端服务
3. 观察 Agent 和前端行为

**预期结果**:
- [ ] Agent 自动重新注册
- [ ] 前端 WebSocket 自动重连
- [ ] 数据恢复正常

---

## 📊 测试报告模板

### 测试执行记录

| 测试编号 | 测试名称 | 执行日期 | 执行人 | 结果 | 备注 |
|---------|---------|---------|--------|------|------|
| 1 | 数据库连接测试 | 2026-02-07 | - | ✅ 通过 | - |
| 2 | 后端服务启动 | 2026-02-07 | - | ✅ 通过 | - |
| 3 | API 接口测试 | 2026-02-07 | - | ✅ 通过 | - |
| ... | ... | ... | ... | ... | ... |

### 问题跟踪

| 问题编号 | 问题描述 | 严重程度 | 状态 | 负责人 | 解决方案 |
|---------|---------|---------|------|--------|---------|
| BUG-001 | 示例问题 | 高 | 已修复 | - | - |

---

## ✅ 测试检查清单

### 功能测试
- [ ] 用户登录/登出
- [ ] Agent 注册
- [ ] 数据采集
- [ ] 数据上报
- [ ] 实时监控
- [ ] 历史查询
- [ ] 数据导出
- [ ] 节点管理

### 性能测试
- [ ] API 响应时间
- [ ] WebSocket 并发
- [ ] 大数据量查询
- [ ] 长时间运行

### 稳定性测试
- [ ] 网络中断恢复
- [ ] 服务重启恢复
- [ ] 异常处理
- [ ] 内存泄漏检查

### 兼容性测试
- [ ] Chrome 浏览器
- [ ] Firefox 浏览器
- [ ] Safari 浏览器
- [ ] Edge 浏览器

---

## 📝 测试最佳实践

1. **自动化优先**: 尽可能编写自动化测试脚本
2. **持续测试**: 每次代码变更后运行测试
3. **记录问题**: 详细记录发现的问题
4. **回归测试**: 修复问题后重新测试
5. **性能基准**: 建立性能基准并持续监控

---

## 🔧 测试工具推荐

- **API 测试**: Postman, curl, httpie
- **性能测试**: Apache Bench, wrk, k6
- **WebSocket 测试**: wscat, Postman
- **浏览器测试**: Chrome DevTools
- **监控工具**: Grafana, Prometheus

---

**测试愉快！** 🎉
