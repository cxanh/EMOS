# 测试指南 - 分布式感知运维系统

**版本**: 2.0  
**更新时间**: 2026-02-12

---

## 📋 测试概览

本文档提供完整的系统测试指南，包括单元测试、集成测试、端到端测试和性能测试。

**最新更新**:
- ✅ AI智能分析功能测试
- ✅ 告警系统完整测试
- ✅ 通知日志功能测试
- ✅ 密码安全功能测试
- ✅ 修改密码功能测试

---

## 🚀 快速测试（推荐）

### 自动化测试脚本

项目提供了自动化测试脚本，可以快速检查系统状态：

**Windows (推荐使用 PowerShell 中文版)**:
```powershell
# PowerShell 中文版（推荐，支持中文显示）
.\test-all-cn.ps1

# CMD 英文版（避免乱码）
.\test-all.bat
```

**Linux/Mac**:
```bash
chmod +x test-all.sh
./test-all.sh
```

**注意事项**:
- Windows CMD 可能显示中文乱码，建议使用 PowerShell 运行 `test-all-cn.ps1`
- 如果 PowerShell 提示执行策略错误，运行：`Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned`
- `test-all.bat` 使用英文输出，避免乱码问题

**测试内容**:
- ✅ 后端数据库连接
- ✅ 后端依赖完整性
- ✅ 前端依赖完整性
- ✅ Agent Python 语法
- ✅ Python 依赖安装

**预期输出**:
```
========================================
EOMS 系统测试脚本
========================================

[1/5] 测试后端数据库连接...
✅ 后端连接测试通过

[2/5] 检查后端依赖...
✅ 后端依赖完整

[3/5] 检查前端依赖...
✅ 前端依赖完整

[4/5] 检查 Agent Python 语法...
✅ Agent 语法检查通过

[5/5] 检查 Python 依赖...
✅ psutil 已安装
✅ requests 已安装
✅ pyyaml 已安装

========================================
测试完成！
========================================
```

**测试结果**:
- 如果所有测试通过，可以继续启动系统
- 如果测试失败，请查看 [测试报告.md](./测试报告.md) 和 [问题修复清单.md](./问题修复清单.md)

---

## 🧪 测试环境准备

### 1. 安装依赖

```bash
# 后端依赖
cd backend
npm install

# 前端依赖
cd frontend
npm install

# Agent 依赖
cd agent
pip install -r requirements.txt
```

### 2. 启动基础服务

```bash
# 启动 Redis (可选)
redis-server

# 启动 InfluxDB (可选)
influxd
```

### 3. 配置环境变量

确保 `backend/.env` 配置正确：
```env
PORT=3000
JWT_SECRET=your-secret-key
REDIS_HOST=localhost
REDIS_PORT=6379
INFLUX_URL=http://localhost:8086
INFLUX_TOKEN=your-token
INFLUX_ORG=eoms
INFLUX_BUCKET=metrics
```

---

## 🔧 后端测试

### 测试 1: 数据库连接测试

**目的**: 验证 Redis 和 InfluxDB 连接

```bash
cd backend
node test-connection.js
```

**预期结果**:
```
✓ Redis 连接成功
✓ InfluxDB 连接成功
```

**故障排查**:
- Redis 连接失败: 检查 Redis 服务是否启动
- InfluxDB 连接失败: 检查 Token 和 URL 配置

---

### 测试 2: 后端服务启动测试

**目的**: 验证后端服务正常启动

```bash
cd backend
npm run dev
```

**预期结果**:
```
Server running on port 3000
✓ Redis connected
✓ InfluxDB connected
WebSocket server ready
```

**验证检查**:
- [ ] 服务在 3000 端口启动
- [ ] Redis 连接成功
- [ ] InfluxDB 连接成功
- [ ] WebSocket 服务就绪

---

### 测试 3: API 接口测试

#### 3.1 健康检查

```bash
        curl http://localhost:3000/health
```

**预期响应**:
```json
{
  "status": "ok",
  "timestamp": "2026-02-07T10:00:00.000Z"
}
```

#### 3.2 用户登录

```bash
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "password": "admin"
  }'
```

**预期响应**:
```json
{
  "success": true,
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "id": "1",
    "username": "admin"
  }
}
```

**保存 Token**: 将返回的 token 用于后续请求

#### 3.3 Agent 注册

```bash
curl -X POST http://localhost:3000/api/agent/register \
  -H "Content-Type: application/json" \
  -d '{
    "nodeId": "test-node-001",
    "hostname": "test-machine",
    "ip": "192.168.1.100",
    "os": "Linux",
    "arch": "x64"
  }'
```

**预期响应**:
```json
{
  "success": true,
  "message": "Agent registered successfully",
  "nodeId": "test-node-001"
}
```

#### 3.4 上报监控数据

```bash
curl -X POST http://localhost:3000/api/agent/metrics \
  -H "Content-Type: application/json" \
  -d '{
    "nodeId": "test-node-001",
    "timestamp": "2026-02-07T10:00:00.000Z",
    "cpu": {
      "usage": 45.2,
      "cores": 8
    },
    "memory": {
      "total": 16384,
      "used": 8192,
      "usage": 50.0
    },
    "disk": {
      "total": 512000,
      "used": 256000,
      "usage": 50.0
    },
    "network": {
      "bytesIn": 1024000,
      "bytesOut": 512000
    }
  }'
```

**预期响应**:
```json
{
  "success": true,
  "message": "Metrics received"
}
```

#### 3.5 获取节点列表

```bash
curl http://localhost:3000/api/agent/list
```

**预期响应**:
```json
{
  "success": true,
  "nodes": [
    {
      "nodeId": "test-node-001",
      "hostname": "test-machine",
      "ip": "192.168.1.100",
      "status": "online",
      "lastSeen": "2026-02-07T10:00:00.000Z"
    }
  ]
}
```

#### 3.6 获取最新数据

```bash
curl http://localhost:3000/api/metrics/latest/test-node-001
```

**预期响应**:
```json
{
  "success": true,
  "data": {
    "nodeId": "test-node-001",
    "timestamp": "2026-02-07T10:00:00.000Z",
    "cpu": { "usage": 45.2 },
    "memory": { "usage": 50.0 },
    "disk": { "usage": 50.0 }
  }
}
```

#### 3.7 查询历史数据

```bash
curl "http://localhost:3000/api/metrics/history?nodeId=test-node-001&startTime=2026-02-07T09:00:00Z&endTime=2026-02-07T10:00:00Z"
```

**预期响应**:
```json
{
  "success": true,
  "data": [
    {
      "timestamp": "2026-02-07T09:00:00.000Z",
      "cpu_usage": 45.2,
      "memory_usage": 50.0,
      "disk_usage": 50.0
    }
  ]
}
```

---

### 测试 4: WebSocket 连接测试

使用浏览器控制台或 WebSocket 客户端工具：

```javascript
// 在浏览器控制台执行
const ws = new WebSocket('ws://localhost:3000/ws/metrics');

ws.onopen = () => {
  console.log('✓ WebSocket 连接成功');
  
  // 订阅节点数据
  ws.send(JSON.stringify({
    type: 'subscribe',
    nodeId: 'test-node-001'
  }));
};

ws.onmessage = (event) => {
  const data = JSON.parse(event.data);
  console.log('收到数据:', data);
};

ws.onerror = (error) => {
  console.error('✗ WebSocket 错误:', error);
};

ws.onclose = () => {
  console.log('WebSocket 连接关闭');
};
```

**预期结果**:
- 连接成功
- 订阅成功
- 收到实时数据推送

---

## 🤖 Agent 测试

### 测试 5: Agent 启动测试

**目的**: 验证 Agent 正常启动和数据采集

```bash
cd agent
python agent.py
```

**预期输出**:
```
EOMS Agent starting...
Node ID: node001
Hostname: your-machine
Agent registered successfully
✓ CPU: 45.2% | MEM: 68.5% | DISK: 72.3%
✓ CPU: 46.1% | MEM: 68.7% | DISK: 72.3%
...
```

**验证检查**:
- [ ] Agent 成功启动
- [ ] 自动注册成功
- [ ] 每秒采集一次数据
- [ ] 数据成功上报
- [ ] 无错误日志

---

### 测试 6: Agent 配置测试

**目的**: 验证配置文件正确加载

编辑 `agent/config.yaml`:
```yaml
server:
  url: http://localhost:3000
  
agent:
  node_id: custom-node-001
  interval: 2
```

重启 Agent，验证：
- [ ] 使用自定义 node_id
- [ ] 采集间隔为 2 秒

---

### 测试 7: Agent 重试机制测试

**目的**: 验证网络异常时的重试机制

1. 启动 Agent
2. 停止后端服务
3. 观察 Agent 日志

**预期行为**:
```
✗ Failed to upload metrics: Connection refused
Retrying... (1/3)
✗ Failed to upload metrics: Connection refused
Retrying... (2/3)
```

4. 重启后端服务
5. Agent 应自动恢复上报

---

## 🎨 前端测试

### 测试 8: 前端启动测试

**目的**: 验证前端应用正常启动

```bash
cd frontend
npm run dev
```

**预期结果**:
```
VITE v5.x.x ready in xxx ms

➜  Local:   http://localhost:5174/
➜  Network: use --host to expose
```

**验证检查**:
- [ ] 应用在 5174 端口启动
- [ ] 无编译错误
- [ ] 无 TypeScript 错误

---

### 测试 9: 登录功能测试

**步骤**:
1. 访问 http://localhost:5174
2. 输入用户名: `admin`
3. 输入密码: `admin`
4. 点击登录

**预期结果**:
- [ ] 登录成功
- [ ] 跳转到首页
- [ ] Token 保存到 localStorage
- [ ] 侧边栏显示用户名

**故障排查**:
- 登录失败: 检查后端服务是否启动
- 网络错误: 检查 API 地址配置

---

### 测试 10: 实时监控测试

**前置条件**: 至少一个 Agent 在线

**步骤**:
1. 登录系统
2. 点击侧边栏 "监控面板"
3. 选择一个节点

**预期结果**:
- [ ] 节点列表正常显示
- [ ] 节点状态正确 (在线/离线)
- [ ] 实时数据每秒更新
- [ ] CPU/内存/磁盘进度条正常
- [ ] 趋势图表实时更新
- [ ] WebSocket 状态显示 "已连接"

**验证数据**:
- 打开浏览器开发者工具
- 查看 Network → WS 标签
- 应看到持续的数据推送

---

### 测试 11: 历史数据查询测试

**步骤**:
1. 点击侧边栏 "历史查询"
2. 选择节点
3. 选择时间范围 (例如: 最近 1 小时)
4. 点击 "查询"

**预期结果**:
- [ ] 查询按钮显示加载状态
- [ ] 数据成功加载
- [ ] 统计卡片显示正确
- [ ] 四个图表正常显示
- [ ] 数据表格显示完整数据

**测试导出功能**:
1. 点击 "导出 CSV"
2. 验证下载的 CSV 文件
3. 检查数据完整性

---

### 测试 12: 节点管理测试

**步骤**:
1. 在监控面板查看节点列表
2. 停止一个 Agent
3. 等待 30 秒

**预期结果**:
- [ ] 节点状态变为 "离线"
- [ ] 状态指示器变为灰色
- [ ] 最后更新时间停止更新

**恢复测试**:
1. 重启 Agent
2. 观察节点状态

**预期结果**:
- [ ] 节点状态变为 "在线"
- [ ] 数据恢复更新

---

## 🔗 集成测试

### 测试 13: 端到端数据流测试

**目的**: 验证完整的数据流转

**步骤**:
1. 启动 Redis
2. 启动后端服务
3. 启动 Agent
4. 启动前端应用
5. 登录系统
6. 打开监控面板

**验证数据流**:
```
Agent 采集 → HTTP POST → 后端接收 → Redis 缓存 → WebSocket 推送 → 前端显示
                                    ↓
                              InfluxDB 存储
```

**检查点**:
- [ ] Agent 日志显示上报成功
- [ ] 后端日志显示接收数据
- [ ] Redis 中有最新数据 (`redis-cli get node:test-node-001:latest`)
- [ ] 前端实时显示数据
- [ ] InfluxDB 中有历史数据

---

### 测试 14: 多节点并发测试

**目的**: 验证系统支持多节点

**步骤**:
1. 启动 3 个 Agent (不同 node_id)
2. 在前端查看节点列表

**预期结果**:
- [ ] 所有节点正常显示
- [ ] 每个节点数据独立更新
- [ ] 切换节点时数据正确显示
- [ ] 无数据混淆

---

### 测试 15: 长时间运行测试

**目的**: 验证系统稳定性

**步骤**:
1. 启动所有服务
2. 运行 1 小时
3. 观察系统状态

**监控指标**:
- [ ] 内存使用稳定 (无内存泄漏)
- [ ] CPU 使用正常
- [ ] WebSocket 连接稳定
- [ ] 无异常错误日志
- [ ] 数据持续更新

---

## ⚡ 性能测试

### 测试 16: API 响应时间测试

使用 Apache Bench 或类似工具：

```bash
# 测试登录接口
ab -n 1000 -c 10 -p login.json -T application/json \
  http://localhost:3000/api/auth/login

# 测试获取最新数据接口
ab -n 1000 -c 10 \
  http://localhost:3000/api/metrics/latest/test-node-001
```

**性能目标**:
- 登录接口: < 100ms (平均)
- 获取最新数据: < 50ms (平均)
- 历史查询: < 500ms (平均)

---

### 测试 17: WebSocket 并发连接测试

**目的**: 验证 WebSocket 支持多客户端

**步骤**:
1. 打开 10 个浏览器标签页
2. 每个标签页登录并打开监控面板
3. 观察系统表现

**预期结果**:
- [ ] 所有连接正常建立
- [ ] 数据推送正常
- [ ] 服务器资源使用合理

---

### 测试 18: 大数据量查询测试

**前置条件**: 系统运行 24 小时以上

**步骤**:
1. 查询 24 小时历史数据
2. 测量响应时间
3. 检查数据完整性

**性能目标**:
- 查询时间: < 2s
- 数据点数: > 86400 (24h * 3600s)
- 图表渲染: < 1s

---

## 🐛 故障测试

### 测试 19: 网络中断恢复测试

**场景 1: Agent 网络中断**
1. 启动 Agent
2. 断开网络
3. 等待 30 秒
4. 恢复网络

**预期行为**:
- Agent 自动重试
- 网络恢复后自动上报
- 无数据丢失

**场景 2: 前端网络中断**
1. 打开监控面板
2. 断开网络
3. 恢复网络

**预期行为**:
- WebSocket 自动重连
- 数据恢复更新
- 显示连接状态变化

---

### 测试 20: 服务重启测试

**步骤**:
1. 系统正常运行
2. 重启后端服务
3. 观察 Agent 和前端行为

**预期结果**:
- [ ] Agent 自动重新注册
- [ ] 前端 WebSocket 自动重连
- [ ] 数据恢复正常

---

## 📊 测试报告模板

### 测试执行记录

| 测试编号 | 测试名称 | 执行日期 | 执行人 | 结果 | 备注 |
|---------|---------|---------|--------|------|------|
| 1 | 数据库连接测试 | 2026-02-07 | - | ✅ 通过 | - |
| 2 | 后端服务启动 | 2026-02-07 | - | ✅ 通过 | - |
| 3 | API 接口测试 | 2026-02-07 | - | ✅ 通过 | - |
| ... | ... | ... | ... | ... | ... |

### 问题跟踪

| 问题编号 | 问题描述 | 严重程度 | 状态 | 负责人 | 解决方案 |
|---------|---------|---------|------|--------|---------|
| BUG-001 | 示例问题 | 高 | 已修复 | - | - |

---

## ✅ 测试检查清单

### 功能测试
- [ ] 用户登录/登出
- [ ] Agent 注册
- [ ] 数据采集
- [ ] 数据上报
- [ ] 实时监控
- [ ] 历史查询
- [ ] 数据导出
- [ ] 节点管理

### 性能测试
- [ ] API 响应时间
- [ ] WebSocket 并发
- [ ] 大数据量查询
- [ ] 长时间运行

### 稳定性测试
- [ ] 网络中断恢复
- [ ] 服务重启恢复
- [ ] 异常处理
- [ ] 内存泄漏检查

### 兼容性测试
- [ ] Chrome 浏览器
- [ ] Firefox 浏览器
- [ ] Safari 浏览器
- [ ] Edge 浏览器

---

## 📝 测试最佳实践

1. **自动化优先**: 尽可能编写自动化测试脚本
2. **持续测试**: 每次代码变更后运行测试
3. **记录问题**: 详细记录发现的问题
4. **回归测试**: 修复问题后重新测试
5. **性能基准**: 建立性能基准并持续监控

---

## 🔧 测试工具推荐

- **API 测试**: Postman, curl, httpie
- **性能测试**: Apache Bench, wrk, k6
- **WebSocket 测试**: wscat, Postman
- **浏览器测试**: Chrome DevTools
- **监控工具**: Grafana, Prometheus

---

**测试愉快！** 🎉

---

## 🔐 安全功能测试

### 测试 21: 弱口令检测测试

**目的**: 验证登录页面的弱口令检测功能

**步骤**:
1. 访问登录页面
2. 在密码输入框中输入不同强度的密码

**测试用例**:

| 密码 | 预期结果 |
|------|---------|
| `123456` | 显示"弱密码"（红色）+ 弱口令警告 |
| `admin` | 显示"弱密码"（红色）+ 弱口令警告 |
| `password` | 显示"弱密码"（红色）+ 弱口令警告 |
| `Admin2024` | 显示"中等强度"（橙色） |
| `MyP@ssw0rd123` | 显示"强密码"（绿色） |

**验证检查**:
- [ ] 密码强度条实时更新
- [ ] 强度等级正确显示
- [ ] 弱口令警告正确显示
- [ ] 颜色标识正确（红/橙/绿）

---

### 测试 22: 修改密码功能测试

**目的**: 验证设置页面的修改密码功能

**步骤**:
1. 登录系统（用户名: `admin`, 密码: `admin`）
2. 点击侧边栏"设置"
3. 填写修改密码表单

**测试用例 1: 正常修改密码**
- 当前密码: `admin`
- 新密码: `NewPass@2024`
- 确认密码: `NewPass@2024`
- 预期: 显示"密码修改成功！"，表单重置

**测试用例 2: 当前密码错误**
- 当前密码: `wrong`
- 新密码: `NewPass@2024`
- 确认密码: `NewPass@2024`
- 预期: 显示"当前密码不正确"

**测试用例 3: 新密码过短**
- 当前密码: `admin`
- 新密码: `123`
- 确认密码: `123`
- 预期: 提交按钮禁用

**测试用例 4: 密码不一致**
- 当前密码: `admin`
- 新密码: `NewPass@2024`
- 确认密码: `NewPass@2025`
- 预期: 显示"两次输入的密码不一致"

**验证检查**:
- [ ] 密码强度实时检测
- [ ] 弱口令警告显示
- [ ] 密码可见性切换正常
- [ ] 表单验证正确
- [ ] 成功/失败提示正确
- [ ] 修改后能用新密码登录

---

## 🚨 告警系统测试

### 测试 23: 告警规则管理测试

**目的**: 验证告警规则的创建、编辑、删除功能

**步骤 1: 创建告警规则**
1. 登录系统
2. 点击侧边栏"告警管理"
3. 点击"新建规则"按钮
4. 填写规则信息：
   - 规则名称: `CPU使用率过高`
   - 监控节点: `所有节点`
   - 监控指标: `CPU使用率`
   - 条件: `大于 >`
   - 阈值: `80`
   - 持续时间: `60` 秒
   - 通知方式: 勾选 `WebSocket推送`
5. 点击"创建"

**预期结果**:
- [ ] 显示"规则创建成功！"Toast通知
- [ ] 规则列表自动刷新
- [ ] 新规则显示在列表中
- [ ] 规则状态为"启用"（绿色圆点）

**步骤 2: 编辑告警规则**
1. 点击规则卡片的"编辑"按钮
2. 修改阈值为 `70`
3. 点击"保存"

**预期结果**:
- [ ] 显示"规则更新成功！"
- [ ] 规则信息更新

**步骤 3: 禁用/启用规则**
1. 点击规则的"暂停"按钮
2. 观察规则状态变化
3. 再次点击"播放"按钮

**预期结果**:
- [ ] 规则状态切换正确
- [ ] 显示相应的成功提示
- [ ] 禁用时规则卡片变灰

**步骤 4: 删除规则**
1. 点击规则的"删除"按钮
2. 确认删除

**预期结果**:
- [ ] 显示确认对话框
- [ ] 删除后规则从列表消失
- [ ] 显示"规则删除成功！"

---

### 测试 24: 告警触发测试

**目的**: 验证告警能够正确触发

**前置条件**: 
- 至少一个Agent在线
- 创建一个低阈值的告警规则（如CPU > 10%）

**步骤**:
1. 创建测试规则：
   - 规则名称: `测试告警`
   - 监控指标: `CPU使用率`
   - 条件: `大于 > 10`
   - 持续时间: `5` 秒
   - 通知方式: `WebSocket推送`
2. 等待5-10秒
3. 切换到"活动告警"标签

**预期结果**:
- [ ] 活动告警列表显示新告警
- [ ] 告警卡片显示红色背景
- [ ] 显示正确的节点、指标、当前值、阈值
- [ ] 显示触发时间
- [ ] 浏览器可能收到通知（如果支持）

**验证数据**:
- 打开浏览器控制台
- 查看WebSocket消息
- 应看到类型为 `alert` 的消息

---

### 测试 25: 告警处理测试

**目的**: 验证告警的处理和忽略功能

**步骤 1: 处理告警**
1. 在活动告警列表中找到一个告警
2. 点击"处理"按钮
3. 输入处理备注（可选）
4. 确认

**预期结果**:
- [ ] 告警从活动列表消失
- [ ] 显示处理成功提示

**步骤 2: 忽略告警**
1. 在活动告警列表中找到一个告警
2. 点击"忽略"按钮
3. 确认

**预期结果**:
- [ ] 告警从活动列表消失
- [ ] 显示忽略成功提示

---

### 测试 26: 通知日志测试

**目的**: 验证通知日志记录和查看功能

**步骤 1: 查看通知日志**
1. 进入"告警管理"页面
2. 点击"通知日志"标签
3. 查看日志列表

**预期结果**:
- [ ] 显示统计卡片（总通知数、成功数、失败数等）
- [ ] 显示日志列表
- [ ] 每条日志显示：
  - 规则名称
  - 节点信息
  - 通知渠道及状态
  - 时间戳

**步骤 2: 验证日志内容**
1. 触发一个告警（参考测试24）
2. 等待告警触发
3. 切换到"通知日志"标签
4. 查看最新的日志记录

**预期结果**:
- [ ] 新日志出现在列表顶部
- [ ] 显示正确的规则名称
- [ ] 显示正确的节点信息
- [ ] WebSocket渠道显示"✓"（成功）
- [ ] 时间戳正确

**步骤 3: 测试刷新功能**
1. 点击"刷新"按钮
2. 观察数据更新

**预期结果**:
- [ ] 统计数据更新
- [ ] 日志列表更新

**步骤 4: 测试加载更多**
1. 滚动到日志列表底部
2. 点击"加载更多"按钮（如果有）

**预期结果**:
- [ ] 加载更多日志
- [ ] 按钮显示"加载中..."
- [ ] 新日志追加到列表

**验证后端日志**:
```bash
# 运行通知日志测试脚本
cd backend
node test-notification-log.js
```

**预期输出**:
```
✓ Notification log saved successfully
✓ Retrieved X logs
✓ Statistics retrieved
  Total: X
  Success: X
  Failed: X
```

---

## 🤖 AI智能分析测试

### 测试 27: AI服务状态测试

**目的**: 验证AI服务正常运行

**前置条件**: Ollama服务已启动

**步骤**:
1. 检查Ollama服务
```bash
curl http://localhost:11434/api/tags
```

2. 检查后端AI配置
```bash
# 查看 backend/.env
AI_ENABLED=true
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:3b
```

**预期结果**:
- [ ] Ollama服务响应正常
- [ ] 后端配置正确

---

### 测试 28: 系统健康检查测试

**目的**: 验证AI系统健康检查功能

**步骤**:
1. 登录系统
2. 点击侧边栏"AI智能分析"
3. 点击"系统健康检查"卡片
4. 等待15-30秒

**预期结果**:
- [ ] 显示加载动画
- [ ] 分析完成后显示结果：
  - 健康评分（0-100）
  - 健康状态（健康/警告/危险）
  - 问题列表
  - 建议列表
  - 紧急程度
- [ ] 结果卡片有动画效果

**测试不同场景**:
- 所有节点正常：健康评分应 > 80
- 有节点CPU高：应识别出CPU问题
- 有节点内存高：应识别出内存问题

---

### 测试 29: 性能趋势分析测试

**目的**: 验证AI性能趋势分析功能

**前置条件**: 系统运行至少1小时，有历史数据

**步骤**:
1. 在AI智能分析页面
2. 点击"性能趋势分析"卡片
3. 选择一个节点
4. 选择时间范围（如"最近24小时"）
5. 点击"开始分析"
6. 等待20-30秒

**预期结果**:
- [ ] 显示加载状态
- [ ] 分析完成后显示：
  - 趋势类型（上升/下降/稳定）
  - 模式识别
  - 预测数据
  - 异常检测
  - 洞察建议
- [ ] 数据可视化展示

---

### 测试 30: 优化建议测试

**目的**: 验证AI优化建议功能

**步骤**:
1. 在AI智能分析页面
2. 点击"优化建议"卡片
3. 等待15-25秒

**预期结果**:
- [ ] 显示加载状态
- [ ] 分析完成后显示：
  - 按类别分组的建议
  - 每个建议的优先级
  - 影响程度
  - 实施难度
  - 快速见效建议
  - 长期优化建议

---

### 测试 31: AI超时处理测试

**目的**: 验证AI请求超时处理

**步骤**:
1. 停止Ollama服务
2. 尝试进行AI分析
3. 观察错误处理

**预期结果**:
- [ ] 显示友好的错误提示
- [ ] 不会导致页面崩溃
- [ ] 可以重试

---

## 🔗 完整集成测试

### 测试 32: 端到端完整工作流测试

**目的**: 验证从监控到告警到分析的完整流程

**步骤**:
1. 启动所有服务（Redis、InfluxDB、Ollama、后端、Agent、前端）
2. 登录系统
3. 创建告警规则（低阈值）
4. 等待告警触发
5. 查看活动告警
6. 查看通知日志
7. 进行AI健康检查
8. 查看优化建议
9. 处理告警
10. 修改密码测试

**验证完整数据流**:
```
Agent采集 → 后端接收 → Redis缓存 → WebSocket推送 → 前端显示
                    ↓
              InfluxDB存储
                    ↓
              告警检查 → 触发告警 → 发送通知 → 记录日志
                    ↓
              AI分析 → 生成建议
```

**检查点**:
- [ ] 数据采集正常
- [ ] 实时监控正常
- [ ] 告警触发正常
- [ ] 通知发送正常
- [ ] 日志记录正常
- [ ] AI分析正常
- [ ] 密码修改正常

---

## ✅ 更新后的测试检查清单

### 功能测试
- [ ] 用户登录/登出
- [ ] 弱口令检测
- [ ] 修改密码
- [ ] Agent 注册
- [ ] 数据采集
- [ ] 数据上报
- [ ] 实时监控
- [ ] 历史查询
- [ ] 数据导出
- [ ] 节点管理
- [ ] 告警规则管理
- [ ] 告警触发
- [ ] 告警处理
- [ ] 通知日志
- [ ] AI健康检查
- [ ] AI趋势分析
- [ ] AI优化建议

### 性能测试
- [ ] API 响应时间
- [ ] WebSocket 并发
- [ ] 大数据量查询
- [ ] 长时间运行
- [ ] AI分析响应时间

### 稳定性测试
- [ ] 网络中断恢复
- [ ] 服务重启恢复
- [ ] 异常处理
- [ ] 内存泄漏检查
- [ ] AI服务异常处理

### 安全测试
- [ ] 弱口令检测
- [ ] 密码强度验证
- [ ] 密码修改功能
- [ ] Token验证
- [ ] 权限控制

### 兼容性测试
- [ ] Chrome 浏览器
- [ ] Firefox 浏览器
- [ ] Safari 浏览器
- [ ] Edge 浏览器

---

## 📊 测试报告模板（更新版）

### 测试执行记录

| 测试编号 | 测试名称 | 执行日期 | 执行人 | 结果 | 备注 |
|---------|---------|---------|--------|------|------|
| 1 | 数据库连接测试 | 2026-02-12 | - | ✅ 通过 | - |
| 21 | 弱口令检测测试 | 2026-02-12 | - | ✅ 通过 | - |
| 22 | 修改密码功能测试 | 2026-02-12 | - | ✅ 通过 | - |
| 23 | 告警规则管理测试 | 2026-02-12 | - | ✅ 通过 | - |
| 24 | 告警触发测试 | 2026-02-12 | - | ✅ 通过 | - |
| 25 | 告警处理测试 | 2026-02-12 | - | ✅ 通过 | - |
| 26 | 通知日志测试 | 2026-02-12 | - | ✅ 通过 | - |
| 27 | AI服务状态测试 | 2026-02-12 | - | ✅ 通过 | - |
| 28 | 系统健康检查测试 | 2026-02-12 | - | ✅ 通过 | - |
| 29 | 性能趋势分析测试 | 2026-02-12 | - | ✅ 通过 | - |
| 30 | 优化建议测试 | 2026-02-12 | - | ✅ 通过 | - |
| 32 | 完整工作流测试 | 2026-02-12 | - | ✅ 通过 | - |

---

## 🎯 测试优先级

### P0 - 核心功能（必须通过）
- 用户登录
- 数据采集和上报
- 实时监控
- 告警触发
- 通知发送

### P1 - 重要功能（应该通过）
- 告警规则管理
- 通知日志
- 历史查询
- AI健康检查
- 密码修改

### P2 - 增强功能（可以通过）
- AI趋势分析
- AI优化建议
- 弱口令检测
- 数据导出

---

## 📝 测试最佳实践（更新）

1. **自动化优先**: 尽可能编写自动化测试脚本
2. **持续测试**: 每次代码变更后运行测试
3. **记录问题**: 详细记录发现的问题
4. **回归测试**: 修复问题后重新测试
5. **性能基准**: 建立性能基准并持续监控
6. **安全测试**: 定期进行安全测试
7. **用户体验**: 关注用户体验和界面友好性

---

## 🔧 测试工具推荐（更新）

- **API 测试**: Postman, curl, httpie
- **性能测试**: Apache Bench, wrk, k6
- **WebSocket 测试**: wscat, Postman
- **浏览器测试**: Chrome DevTools
- **监控工具**: Grafana, Prometheus
- **AI测试**: Ollama CLI, curl
- **安全测试**: OWASP ZAP, Burp Suite

---

**测试愉快！** 🎉

**最后更新**: 2026-02-12  
**版本**: 2.0
